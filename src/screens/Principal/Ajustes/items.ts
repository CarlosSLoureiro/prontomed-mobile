import { ItemContrato } from '@components/Ajustes/Item/types';

import { Ajustes } from './enums';

const items: Array<ItemContrato> = [
  {
    nome: 'Adicionar consultas no calendário',
    variavel: Ajustes.CALENDARIO
  }
];

export default items;
